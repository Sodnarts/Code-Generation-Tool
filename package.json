{
    "name": "cgt",
    "version": "1.0.0",
    "description": "Code Generation Tool",
    "main": "CodeGenerationTool.js",
    "dependencies": {
        "axios": "^0.21.1",
        "readline": "^1.3.0"
    },
    "devDependencies": {
        "tslint": "^6.1.2",
        "typescript": "^3.9.3"
    },
    "__comment1__": "Use `npm run --silent interface` to prevent npm from printing errors when linter find errors. The `--silent` prevents this issue.",
    "__comment2__": "Linter is set to only use `warning` instead of `error`, if you prefer to differentiate this, use the `--silent` flag as specified above.",
    "scripts": {
        "lint": "tslint -c tslint.json -p .",
        "start": "node CodeGenerationTool.js & tslint -c tslint.json -p ."
    },
    "author": "Glenn Robert Strandos",
    "license": "ISC"
}
